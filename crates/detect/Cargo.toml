[package]
name = "uu-detect"
version = "0.1.0"
edition = "2021"
description = "Project type detection for uu — universal utilities"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
